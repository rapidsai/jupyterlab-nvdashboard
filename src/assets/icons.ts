import { LabIcon } from '@jupyterlab/ui-components';
import gpuIconStr from '../../style/expansion-card.svg';

export const gpuIcon = new LabIcon({
  name: 'launcher:python-icon',
  svgstr: gpuIconStr
});
